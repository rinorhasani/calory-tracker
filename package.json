{
  "name": "calai",
  "version": "1.0.0",
  "private": true,
  "description": "CalAI – AI Calorie Tracker (Capacitor iOS shell)",
  "scripts": {
    "sync:web": "cp index.html manifest.json sw.js www/",
    "ios:open": "npm run sync:web && npx cap copy ios && npx cap open ios"
  },
  "dependencies": {
    "@capacitor/core": "^6.1.0",
    "@capacitor/ios": "^6.1.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.1.0"
  }
}
